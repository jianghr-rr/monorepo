# 箭头函数  

## 上下文

``` javascript
const obj = {
    name: 'obj',
    getName: () => {
        console.log(this.name);
    }
}
```

**dom操作的callback**
``` javascript
const $a = document.getElementById('a');

a.addEventListener('click', () => {
    this.style.color = 'red';
})
```
**类操作**
``` javascript
```
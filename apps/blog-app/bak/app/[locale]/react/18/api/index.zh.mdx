# 内置 React API

- createContext 允许您定义子组件并提供上下文。与 useContext 一起使用.
- forwardRef 允许组件将 DOM 节点作为引用公开给父节点。与 useRef .
- lazy 允许您将组件代码的加载推迟到首次呈现。
- memo 让您的组件跳过使用相同道具的重新渲染。与 useMemo 和 useCallback 一起使用。
- startTransition 允许您将状态更新标记为非紧急。与 useTransition 相似。
- act 允许您在测试中包装呈现和交互，以确保在做出断言之前已处理更新。
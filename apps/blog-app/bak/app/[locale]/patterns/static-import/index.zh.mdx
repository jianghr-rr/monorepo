# 静态导入

import关键字允许我们导入已由另一个模块导出的代码。默认情况下，我们静态导入的所有模块都会添加到初始包中。使用默认 ES2015 导入语法`import module from 'module'`导入的模块是静态导入的。

一旦引擎到达我们导入它们的行，这些模块就会立即执行。当你打开控制台时，你可以看到模块加载的顺序！

由于组件是静态导入的，Webpack 将模块捆绑到初始包中。

大的包大小可能会显着影响应用程序的加载时间，具体取决于用户的设备和网络连接。在App组件能够将其内容呈现到用户屏幕之前，它首先必须加载并解析所有模块。
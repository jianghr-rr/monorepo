import Link from 'next/link';

# 伪协议
- data伪协议
- javascript伪协议
它们都是用于在浏览器中执行特定操作或访问数据的URL方案。这些伪协议并不真正用于传输数据，而是通过浏览器的解释器执行特定的动作或访问数据。

## data伪协议

data伪协议允许将数据直接嵌入到URL中，而不是引用外部资源。它的一般形式如下：

``` wasm
data:[<MIME-type>][;base64],<data>
```

- `<MIME-type>`: 可选参数，指定数据的MIME类型，例如"text/plain", "image/jpeg"等。
- ;base64: 可选参数，指示数据是否采用base64编码。
- `<data>`: 实际的数据内容。


## javascript伪协议

javascript: 伪协议用于执行JavaScript代码。当浏览器遇到以"javascript:"开头的URL时，会直接执行其后的JavaScript代码。

``` html
javascript:alert('Hello, World!');
```

### 主要区别和用途
- data: 伪协议主要用于将数据内联到URL中，常用于在页面中嵌入小型资源或者测试目的。
- javascript: 伪协议用于执行JavaScript代码，可用于创建简单的交互效果、修改页面内容或执行其他DOM操作。

## Unicode编码

ISO制定的所有地球上符号，文字的编码，它用两个字节来表示一个字符

Unicode是一个符号集，它只规定了符号的二进制代码。没有规定这个二进制代码应该如何存储。

具体存储： UTF-8， UTF-16等实现。

## Unicode编码（JavaScript编码）和URL编码

- JavaScript编码
- HTML编码
- URL编码

## 浏览器解码

HTML解析， URL解析，JS解析，每个解析的过程都需要解码。

解码的顺序是先进行HTML解码，再进行URL解码，再进行JS解码

浏览器接收到HTML先解析HTML文档，然后解析标签的属性，属性可能有URL，URL内部可能存在其他的伪协议。